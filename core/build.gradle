apply plugin: "java"
apply plugin: "scala"

sourceCompatibility = 1.6

sourceSets.main.java.srcDirs = [ "src/" ]
sourceSets.main.scala.srcDirs = [ "src/" ]

eclipse.project {
    name = appName + "-core"
}
